# Get rid of uneeded user folders

The names of the files speak for themselves. 
To hide the specific folder launch the .reg file with Remove in name. 
To restore the folder use .reg files with Add in name.

